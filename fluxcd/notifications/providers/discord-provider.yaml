---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: discord
  namespace: default
spec:
  channel: alerts
  secretRef:
    name: discord
  type: discord
  username: FluxBot
